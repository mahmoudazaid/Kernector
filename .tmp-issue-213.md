TITLE: Add LangGraph short-term and long-term memory

## Context

`135.md` medium optional #2: implement long-term or short-term memory in LangChain/LangGraph. Today Streamlit holds chat messages in `st.session_state` only — no graph checkpoint or durable memory store.

## Goal

Add short-term thread/checkpoint memory for the Sprint 3 agent path, and optionally a long-term memory store for cross-session facts.

## Acceptance criteria

- [ ] Agent turns use a LangGraph (or equivalent) checkpoint so follow-ups reuse prior state within a thread
- [ ] UI can start a new thread / clear short-term memory
- [ ] Optional: long-term store (e.g. user/project facts) with clear read/write boundaries behind ports
- [ ] No LangGraph imports in `domain` / `application`
- [ ] Tests cover checkpoint reuse and clear/reset

## Depends on

- `#43` / LangGraph agent path under Epic `#211`

## Spec

`135.md` Optional → Medium #2; `docs/sprint-3-135-review.md`.